info:
  name: get all developers
  type: http
  seq: 1

http:
  method: GET
  url: "{{DevelopersEndpoint}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
